#include <iostream>
using namespace std;
int main(){
   unsigned long long int n,c=0;
    cin >> n;
    while(n)
        {
        c+=n&1ULL?0:1;
        n/=2;
    }
    cout<<pow(2,c);
    return 0;
}
